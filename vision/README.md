# Deprecated — use `cv/` instead

This package is a **legacy stub** (`DummyDetector`, `CVNavigator`) from an early prototype.

**Active computer vision:** [`cv/`](../cv/) — YOLOv8 segmentation, OAK-D, vineyard row navigation.

Do not add new imports from `vision`. The package remains only to avoid breaking old references; it will be removed after Sprint 1.
