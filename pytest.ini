[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --tb=short
markers =
    slow: needs ultralytics/torch (optional local run)

# ROS Humble: перед pytest виконайте
#   export PYTEST_DISABLE_PLUGIN_AUTOLOAD=1
# або:  bash scripts/run_tests.sh
